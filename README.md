### Liontunnel
# INSTALL SCRIPT 
```
apt update && apt upgrade -y && apt install build-essential -y && apt-get install -y jq && apt-get install shc && apt install -y bzip2 gzip coreutils screen curl && wget https://raw.githubusercontent.com/liontunnel/script/main/setup.sh && chmod +x setup.sh && ./setup.sh
```
